1# config.txt
BOX_WIDTH = 800
BOX_HEIGHT = 800
VIDEO_ASSET_PATH = ../../../../../taking_stock_production/bin_sorting/videos
ARRANGEMENTS_PATH = ../../../../../taking_stock_production/bin_sorting/arrangements
VIDEO_LOOP = false  # loop videos or swap to new video when finished

# Layout transition: jumpcut | fade | jumpcut_to_black
TRANSITION_TYPE = fade
TRANSITION_DURATION_FADE = 0.5    # seconds for fade down/up
TRANSITION_DURATION_JUMP_TO_BLACK = 0.5  # seconds to hold black before swap
TRANSITION_TIMER_MIN = 3    # min seconds between auto-transitions
TRANSITION_TIMER_MAX = 40    # max seconds between auto-transitions

### CONTROLS ###
#s: export image 
#r: regenerate layout

# wr hr weight expandX expandY
SIZE_RATIO = 1 1 0.2 0.1 0.1
#SIZE_RATIO = 1 2 0.1 0.2 0.2
SIZE_RATIO = 2 3 0.2 0.1 0.1
#SIZE_RATIO = 1 3 0.05 0.2 0.2
#SIZE_RATIO = 2 1 0.1 0.2 0.2
SIZE_RATIO = 3 2 0.2 0.1 0.1
#SIZE_RATIO = 3 1 0.05 0.2 0.2


GAP_FILTER_THRESHOLD = 10 #reject layouts where largest empty rect >= this (px²). 0 = only perfect fill
PACKING_STOP_AREA = 10000 #stop placing when largest placeable item would be < this (px²); prevents infinite tiny items
NESTING_LAYERS = 0
NESTED_MIN_SPACE_THRESHOLD = 0
MAIN_BIN_FILL_CHANCE = 0.05
ITEM_BREAK_SCALE = 0.35 #How big does an object have to be to have a chance to break?
ITEM_BREAK_CHANCE = 0.75 #what is the chance to break?
BREAK_BOX_MIN_ITEMS = 1 #How many items to break into?
BREAK_BOX_MAX_ITEMS = 4 #How many items to break into?
BREAK_BOX_FILL_ATTEMPTS = 5 #How many attempts to break before lowering number of items
BREAK_BOX_COVERAGE_THRESHOLD = 0.99 #What percentage of the original size has to be filled to pass?

#Layout generation tuning (for more thorough discovery):
LAYOUT_MAX_ATTEMPTS = 50000   # max sort() calls per phase (default 50k)
LAYOUT_STALE_THRESHOLD = 3000 # stop phase after this many duplicates (default 3k)
LAYOUT_PHASES = 3            # reseeded phases to explore different regions (default 5)
PLACEMENT_AREA_EXPONENT = 1.4 # score = area^exp * weight; >1 favors larger items with less weight
PLACEMENT_TOP_K = 3          # randomly pick from top K candidates for variation (1=always best) vastly increases num arrangements
